# Web App

Place the React web app here.

Recommended options:

- `Next.js` if the web app needs app routing, SSR, and strong SEO control
- `React + Vite` if this is purely an authenticated app shell and the landing page handles SEO
