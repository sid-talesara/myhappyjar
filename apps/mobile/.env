# Tell Expo CLI to use apps/mobile as the Metro server root (not workspace root).
# Without this, Metro computes bundle paths relative to the monorepo root,
# producing /apps/mobile/index.bundle which Expo Go cannot find (expects /index.bundle).
EXPO_NO_METRO_WORKSPACE_ROOT=1
